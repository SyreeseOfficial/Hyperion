Open http://localhost:3000 in your browser.

For future sessions: from the app/ directory run npm run dev yourself (or ! npm run dev right here in the prompt to pipe it into the conversation). The server hot-reloads on file saves, so you can leave it running while we build.
